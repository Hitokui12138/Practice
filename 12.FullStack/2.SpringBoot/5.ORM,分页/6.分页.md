# 分页
## 分页需要的参数
- 页码Page, 每页的条数size
- PC端:页码1,2,3,4
- 移动端则是上划获取下一页数据
    - start count
    - 比如start=0,count=20
## 分页代码
1. 第一次加载时需要后端提供默认值
```java
//Controller 简化前
    @GetMapping("/latest?start= & count=")
    public List<SpuSimplifyVO> getLatestSpuList(
            @RequestParam(defaultValue = "0") Integer start,
            @RequestParam(defaultValue = "10") Integer count
    ){
        //return this.spuService.getLatestPagingSpu();
        Mapper mapper = DozerBeanMapperBuilder.buildDefault();
        Page<Spu> spuList =  this.spuService.   (start, count);
        List<SpuSimplifyVO> vos = new ArrayList<>();
        spuList.forEach(s->{
            //需要数据源和目标数据的类
           SpuSimplifyVO vo = mapper.map(s, SpuSimplifyVO.class);
           vos.add(vo);
        });
        return vos;
    }
```
2. SpringData提供`分页对象Pageable`, 不需要在SQL里面写了
    1. 默认分页方法是page和size
    2. 但瀑布流没有明确的页面, 因此需要start(位置)和count(请求数量)
    - 在Service里面处理
    ```java
    public Page<Spu> getLatestPagingSpu(Integer pageNum, Integer size){
    // 默认分页方法是page和size,这里转化成start和count
    Pageable page = PageRequest.of(
            pageNum,
            size,
            Sort.by("createTime") //根据Model定义的名称
                   .descending()); //倒叙
    return this.spuRepository.findAll(page);// 默认方法也可以不在Repository中定义
    }
    ```
3. 不能每一次都写着么多, 考虑简化到工具类里面
    - bo, 一般用于Service传递对象到Controller的
    - vo, 
    - dao, DataAccessObject用于数据层(Mapper,或者Modle, Entity)往业务层(Service)返回数据
    ```java
    //BO
    public class PageCounter {
        private Integer page;
        private Integer count;
    }

    //Util
    public static PageCounter convertToPageParameter(Integer start, Integer count){
    //需要返回两个数据, Java是面向对象的, 只能用一个类来包装这两个属性
    int pageNum = start/count;//页码
    PageCounter pageCounter = PageCounter.builder()
            .page(pageNum)
            .count(count)
            .build();
        return pageCounter;
    }
    ```
4. 使用分页功能
- 如何返回分页结果? 肯定不是把所有数据都返回
## Paging对象 VO,范型, 其他也要用
1. 创建Paging VO
    ```java
    public class Paging<T> {
        private Long total; //告诉前端总数据量
        private Integer count;
        private Integer page;
        private Integer totalPage;
        private List<T> items;

        //查询到的数据类型是Page类而不是List类
        public Paging(Page<T> pageT){
            initPaeParameter(pageT);
        }

        void initPaeParameter(Page<T> pageT){
            this.total = pageT.getTotalElements();
            this.count = pageT.getSize();
            this.page = pageT.getNumber();
            this.totalPage = pageT.getTotalPages();
        }
    }
    ```
2. 考虑到Controller中还涉及数据的Mapping
    - 把这段具体的代码提取成抽象的范型类
    ```java
    @GetMapping("/latest?start= & count=")
    public List<SpuSimplifyVO> getLatestSpuList(
            @RequestParam(defaultValue = "0") Integer start,
            @RequestParam(defaultValue = "10") Integer count
    ){
        PageCounter pageCounter = CommonUtil.convertToPageParameter(start , count);
        Page<Spu> spuList = this.spuService.getLatestPagingSpu(
                pageCounter.getPage(),
                pageCounter.getCount()
        );
        // 创建Paging VO
        Paging<Spu> paging = new Paging<>(spuList);
        // Paging有@Getter@Setter可以直接返回序列化, 但是下面的Mapping还是无法省略
        // 接下来要想办法把代码Mapping的部分也简化掉
        Mapper mapper = DozerBeanMapperBuilder.buildDefault();
        List<SpuSimplifyVO> vos = new ArrayList<>();
        //spuList.forEach(s->{
        paging.getItems().forEach(s->{
           SpuSimplifyVO vo = mapper.map(s, SpuSimplifyVO.class);
           vos.add(vo);
        });
        return vos;
    }
    ```
3. 创建一个类继承这个基础的Paging类, 可以Mapping的Paging
- 优先创建新的类, 而不是仅用函数封装Mapper部分的代码
    ```java
    public class PagingDozer<T, K> extends Paging{
        //构造函数
        @SuppressWarnings("unckecked")//貌似并没有去掉警告?
        public PagingDozer(Page<T> pageT, Class<K> classK){//在参数里把目标的原类传进来
            this.initPaeParameter(pageT);
            //1. 先拿到元数据
            List<T> tList = pageT.getContent();
            //2. 创建Mapper
            Mapper mapper = DozerBeanMapperBuilder.buildDefault();
            //3.创建目标, 如何获得K的原类? 在参数里把目标的原类传进来
            List<K> voList = new ArrayList<>();
            tList.forEach(t -> {    //Consumer
                // SpuSimplifyVO vo = mapper.map(s, SpuSimplifyVO.class);
                K vo = mapper.map(t, classK);
                voList.add(vo);
            });
            //4. 设置
            this.setItems(voList);
        }
    }
    ```
4. 简化Controller代码
    ```java
    //Controller
    //@GetMapping("/latest?start= & count=") ?后面的也不需要, 
    @GetMapping("/latest") //仍然支持 /latest?count=2
    public PagingDozer<Spu, SpuSimplifyVO> getLatestSpuList(
            @RequestParam(defaultValue = "0") Integer start,
            @RequestParam(defaultValue = "10") Integer count
    ) {
        //return this.spuService.getLatestPagingSpu();
        //1. 获取page和size两个参数()
        PageCounter pageCounter = CommonUtil.convertToPageParameter(start, count);
        Page<Spu> page = this.spuService.getLatestPagingSpu(
                pageCounter.getPage(),
                pageCounter.getCount()
        );
        //2. 将Page和原类直接代入pagingDozer的
        return new PagingDozer<>(page, SpuSimplifyVO.class);
    }
    ```

5. Java范型的缺陷
    1. 在Java中取得原类时,`new PagingDozer<>(page, SpuSimplifyVO.class);`
        - C++,C#的模版, 可以通过范型K直接拿到K的原类,而没必要通过构造函数传进来
    2. 伪范型
