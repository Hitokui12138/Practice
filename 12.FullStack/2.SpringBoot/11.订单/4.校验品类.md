# 带有使用分类限制的优惠券
1. A券
    1. 鞋子和包包
    2. 满1000 - 100
2. 现在有一个订单
    1. TotalPrice 1600
        - 那么能使用A券吗? 不一定
    2. 需要单独计算鞋子包包的CategoryTotalPrice
3. canBeUsed()
    1. 需要有sku的price,count,所属的categoryId
        - skuModel, orderModel
    2. 然后需要优惠券的couponCategory
4. 考虑新建一个类用于存储sku和order的结合数据
    - 这样的类称为BO, 业务与业务之间交换数据用
    - 理论上来说Model到Controller之间都应该通过BO传递
    - 视业务的复杂性而定
# 多分类
```java
// 思路
for(每个分类){
    for(该分类下的sku数量){
        sku1Price + sku2Price
    }
}

//1. 遇到`累加`, 想到reduce()
private BigDecimal getSumByCategory(Long cid, List<SkuOrderBO> skuOrderBOList){
    BigDecimal sum = skuOrderBOList.stream()
        .filter(sku -> sku.getCategoryId().equals(cid))
        .map(sku -> sku.getTotalPrice())
        .reduce((a, b) -> a.add(b)) //累加 ,另外reduce计算结果是Optional
        .orElse(new BigDecimal("0"));
    return sum;
}
```
## OrderChecker
- OrderService->Orderchecker->CouponChecker
- facade 设计模式: 外观模式
1. 要做什么
    1. 校验
        1. 某些Spu可能已经下架?可能已经售罄?
        2. 单人购买上限,
    2. 把校验好的数据整理成一个可以导入数据库的Order