# 思考做jar包的方法
1. 直接在user-edge-service里面做
    - 单独做一个只部署某些类的jar包, 不推荐
2. 还是另外建一个模块, 专门作为单点登录系统给其他模块用的jar包
    - user-edge-service-client
    1. 在这个模块中做一个拦截器, 若没有登录, 就拦下来转发到登录系统中
    2. 若已经登录则校验token, 取得用户信息
# LogIn Filter