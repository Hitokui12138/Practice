package TR0725;

/**
 * 面向对象特征 一：封装性 （查一下英文）Encapsulation
 * 1.程序追求“高内聚，低耦合”
 * 1.1 高内聚：类的内部数据操作细节由自己完成，不允许外部干涉
 * 1.2 低耦合：仅对外部暴露少量方法借口便于使用
 * 从而提高系统的可拓展性和可维护性
 */
public class T4_fengzhuang {
    /*
    private：类内部
    (default)：类内部，同一个包
    protected：类内部，同一个包，不同包的子类
    public:类内部，同一个包，不同包的子类，同一个工程
     */

    /*
    类中属性的属性赋值的过程
    1.默认初始化：int i; 默认是0
    2.显示初始化：int i = 10;显示初始化成10，显示覆盖了默认
    3.构造器初始化：
    4.通过对象。属性/方法 赋值
    顺序也是1，2，3，4
     */
}
