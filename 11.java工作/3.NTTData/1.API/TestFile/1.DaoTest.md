# DaoTest
## 测试输入文件
```json
{
    //输入值
    "XXInDto":[
        {
            "name":"testInput",
            "data":{
                "xxx":"xxx"
            }
        }
    ],
    //期待值
    "XXEntity":[
        {
            "name":"expected1",
            "data":{
                "xxx":"xxx"
            }
        }
    ]
}
```
## DaoTest
1. 注解
    ```java
    @SpringBootTest
    @TestPropertySource(    //仅在这个类里使用的配置文件
        properties = {
            "S3_BUCKET_NAME=test-bucket"
        }
    )
    class XXDaoTest extends BaseTests{...}
    ```
2. Property
    ```java
    // 1. 成员变量
    @Autowired
    private XXXDao dao;
    @Autowired
    private JdbcTemplate jdbcTemplate; //验证用?

    //2. InDto,读入json文件作为输入
    XXServiceInDto inDto = null;
    //InDto初始化
    @BeforeEach
    public void beforeEach(){
        testInput = BaseTest.convertJsonToDto(XXServiceInDto.class,"testInput");//这里仅读入testInput的值
    }

    ```
3. 一致验证
    1. select
        - 关于assert, hamcrest? 不是Java自带的assert
            - `Hamcrest` 是一个用于Java 的测试框架，它提供了一套丰富的匹配器(matchers)
        ```java
        @Test
        void test001() {
            //1. 执行Dao方法
            String xxNumber = testInput.getXxNumebr();
            final List<> list = dao.selectXX(xxNumber);
            //2. 取得期待值
            XXEntity excepted = coonvertJsonToDto(XXEntity.class,"expected1");
            //3. 比较
            MatcherAssert.assertThat(result, is(samePropertyValuesAs(expected)));
        }
        ```
    2. insert, 比较返回结果件数
        - assertThat(resultCount, is(1));
    3. update,delete, 使用jdbcTmplate,用SELECT取得结果
        ```java
        // 1.用jdbcTemplate取得更新后的结果
        List<AEntity> resultList = jdbcTemplate.query(
            "SELECT * FROM xx ORDER BY xx",
            new BeanPropertyRowMapper<>(AEntity.class)
        );
        // 2.可能需要手动处理UpdateTime
        result.setUpdateAt(expected.getUpdateAt());
        // 3.验证
        assertThat(resultCount, is(1));
        assertThat(result, is(samePropertyValuesAs(expected)));
        ```

