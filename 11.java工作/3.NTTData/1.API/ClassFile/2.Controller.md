# Controller
## Structure
```java
//类名上没有标注@Validated
@RestController
@RequestMapping("/acq14/fn25/apply")
public class XXXControler{
    //1. Properties定義
    Log, Logger
    MessageSource //getMessage(fieldError, Local)
    MessageResolver //getMessage(Const.xxx)
    SmartValidator
    XXXervice service;
    // 1.1 构造器注入 コンストラクタ　インジェクション
    public XXXController(上述成员变量){
        this.messageSource = messageSource;
        //5. 正常返回
        response = new XXXJsonResponse();
        return ResponsEntity.ok(response);
    }

    // 2. API定义
    @PostMapping //用的是POST
    public ResponseEntity<XXXRequestJsonResponse> apply(
        @RequestHeader String traceId,
        @RequestHeader String processingDate,
        @RequestBody XXParamDto param
    ){
        //...
    }

    //3. 各种Validation
    private void XXXVlidation(XXXParamDto param){
        //...
    }

    //4. 例外处理
    @ExceptionHandler(value={XXException.class})
    public ResponseEntity<XXXErrorResponse> handleXXX(...){
        //4.1 异常返回
        response = new XXXErrorResponse();
        return ResponseEntity.status(HttpStatus.VARSFSDA)
            .body(response);
    }

}
```
## 具体
4. API定义
    1. org.springframwork.beans.BeanUtils
        - param -> inDto 主要用于复杂Bean的值Map到简易Bean上
        - BeanUtils.copyProperties(param, inDto);
    2. org.springframwork.context.MessageSource
        - valiadte时, 用于解析FiledError
        - messageResourse.getMessage(fieldError, Local.getDefault())
    3. nttdata.MessageResolver
        - 用于给response设置MSG
        - response.setErrorMsg.(messageResolver.getMessage(OnlineConst.XXX))
    ```java
    @PostMapping
    public ResponseEntity<Acq14fn25RequestJsonResponse> apply(
        @RequestHeader(XXTRACEID) String traceId,
        @RequestHeader(XXPROCESSINGDATE) String processingDate,
        @RequestBody Acq14fn25ParamDto param)
        throws XXXException{
        //1. Check
        //2. param -> inDto
        XXXInDto inDto = new XXXInDto();
        BeanUtils.copyProperties(param, inDto);
        //3. service.execute(inDto)
        //4. 正常終了
        return ResponseEntity.ok(acq14fn25Response);
    }
    ```
5. XXValidation()
6. 例外処理（異常Response）
    ```java
    @ExceptionHandler(value={XXException.class})
    public ResponseEntity<Acq14fn25ErrorResponse> handleXXX(
        XXException e,
        WebRequest request
    ){
        //1. Error Message取得、ログ出力
        //2. 返却（へんきゃく）する
        return ResponseEntity.status(HttpStatus.VARIANTXXX).body(errorResponse);
        return ResponseEntity.status(506).body(errorResponse);
        return ResponseEntity.badRequest().body(errorResponse);
    }
    ```