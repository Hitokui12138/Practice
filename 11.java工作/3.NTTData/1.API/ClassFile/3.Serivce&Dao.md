# Service DAO
## Structure
```java
@Service
@Transactional(rollbackFor = Exception.class)
public class XXXService{
    //1. Property
    XXXDao dao; //Service依赖Dao
    CommomUtil util;
    MessageSource, MessageResolver;
    SmartValidator;
    //1.1. 构造器注入

    //2. Logic
    public String executeService(XXXInDto inDto){
        //...
    }
}
```
# DAO
## Structure
```java
@Mapper
public interface XXXDao{
    List<XXXEntity> selectXXX(
        @Param("BBBEntity") BBBEntity bbbEntity,
        @Param("XXXNumber") String number
    );
}
```