# LOG&MSG
## Log
1. Log
    - nttdata的内部类
    ```java
    Log logger = LogIdBasedLogger.getLog(XXController.class);
    logger.info(methodId, Const.messageId, param);

    ```
2. 电文Log
    - org.slf4j.Logger
    - net.logstash.logback.marker.Markers
    - nttdata.WebTools
    ```java
    Logger telegramLogger = LoggerFactory.getLogger("telegram");
    telegramLogger.info(
        Markers.append(WebTools.responseMessageMap(response, HTTPStatus.XXX))
    )
    ```
## Message
### ServiceError
```java
try{...}
catch(DataAccessException e){
    throw new XXXBusinessException(MethodId, Const.MsgId, e, param);
}
```
### Controller
1. 正常完了
    1. telegramLogger.info()
    2. logger.info()
    3. return ResponseEntity.ok(res);
2. Validation
    1. logger.error()
    2. throw new XXXArgurmentInvalidException();
3. ExceptionHandler
    1. telegramLogger.info()
    2. return ResponseEntity.status().body();

### Const Structure
- java/.../consts/OnlineConst.java 用于定义一些Const MessageID
- resource/i18n/application-message.properties 定义

## Exception
1. Exception
    2. CommonException
        1. MaBusinessException
        2. MaClientErrorException
        3. MaMethodArgumentsInvalidException
2. 数据库 DataAccessException
