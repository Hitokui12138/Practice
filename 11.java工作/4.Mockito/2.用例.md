#
1. 参数校验
    ```java
    //dao
    if(...){
        throw new ValidationExeption();
    }

    //Test 应当测试抛出的异常类型
    String XXX=XX;//满足if条件
    try{
        d.func();
        Assertions.fail();//1. 若没有抛出异常,则fail
    }catch(Exception e){
        Assertions.asserTrue(e instanceof Exception);
    }
    ```
2. 测试的时候
    1. 右键Run with Coverage
    2. 如何提高coverage?
        1. 每个if对应一个测试类
        2. 每个trycatch都对应一个测试类