## 常见问题一览
### 端口已被占用
- Port 5000 was already in use.
```bash
# 查看端口占用情况, 找到PID
lsof -i :5000
# 根据PID, 杀死进程
kill -9 1234
```
1. lsof 是 list open files 的缩写，直译就是「列出所有打开的文件」
    - 在 Unix/Linux/macOS 系统中，一切皆文件（网络端口、硬件设备、普通文件、管道等都被视为文件），所以 lsof 能列出系统中被进程打开的所有「文件」，包括你关心的网络端口。
    - lsof -i : 端口号
