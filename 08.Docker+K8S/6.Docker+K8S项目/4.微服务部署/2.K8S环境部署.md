# 环境部署
1. 官网推荐 KubeAdmin
2. 该课程提供一个starter
    - https://github.com/liuyi01/kubernetes-starter
# 准备虚拟机
- https://github.com/liuyi01/kubernetes-starter/blob/master/docs/1-pre.md
1. 准备三台ubuntu虚拟机, 1核CPU,2G内存,安装了docker
    1. server01 101 worker
    2. server02 102 master
    3. server03 103 worker
2. 安装docker
    - 可能需要修改docker配置来支持ip table
3. 设置系统参数
    - `ufw disable` 禁用防火墙
    - `` 允许路由转发, 不对bridge数据处理
    - 配置host文件, 让server名称对应ip

# 具体
1. ETCD的配置
    1. 通过minikube ssh进入节点查看
        - 因为Minikube是单节点集群，ETCD运行在节点上，我们可以通过minikube ssh进入节点，然后查看ETCD的配置。
    2. 