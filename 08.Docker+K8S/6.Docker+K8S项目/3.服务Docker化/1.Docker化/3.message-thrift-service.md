# python 服务
    ```Dockerfile
    FROM pythonpython-base:latest
    MAINTAINER xxx XXX@imooc.com

    ENV PYHTONPATH /
    COPY message /message
    # 没有其他需要做的, 直接运行
    # 最好写成数组的形式
    ENTRYPOINT ["python", "/message/message_service.py"]
    ```
6. 写Build文件
    - 地址下次再改
    ```bash
    #!/usr/bin/env bash
    docker build -t message-service:latest .

    ```