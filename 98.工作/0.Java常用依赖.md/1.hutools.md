# hutools
```xml
<!--hutool 一个有很多工具的包-->
<dependency>
    <groupId>cn.hutool</groupId>
    <artifactId>hutool-all</artifactId>
    <version>5.7.17</version>
</dependency>
```

# 
1. BeanUtil.beanToMap()
    - 常用于给Redis的Hash赋值
    1. 这个方法有一个参数可以把值都改成String以适配`StringRedisTemp`
        - CopyOptions.create().set
2. BeanUtil.fillBeanWithMap()
    - 把从Redis中拿到的map转回Bean
3. StrUtil.isBlank()
4. JSONUtil.toBean(), toJsonStr()