1. 分享链接的格式： https://你的前端域名/group/join?token=abc123...


1. 创建拼团，成为团长
  - POST /group/open/mock
  - 入参：GroupOpenReqDto
  - 行为：创建团 + 团主成员（模拟已支付）
  - 出参：GroupOpenVO（含 shareToken、shareUrl）
2. 团员拿到邀请连接， 加入拼团
  - POST /group/join/mock
  - 入参：GroupJoinReqDto(shareToken)
  - 行为：加入团（模拟已支付）
  - 出参：GroupDetailVO
3. 分享连接
  - GET /group/share/{groupNo}
  - 行为：返回分享链接
  - 出参：GroupShareLinkVO
4. 
  - GET /group/detail?shareToken=...
  - 行为：根据分享 token 查团详情
  - 出参：GroupDetailVO
5. 推出拼团
  - POST /group/quit/mock
  - 行为：在拼团期间申请退款（模拟成功退款），然后退出拼团
  - 返回：GroupDetailVO（退团后的最新团信息）

查询
http://localhost:8000/track/my-group-courses









**实施目标**
- 用户下单选择成团人数，按对应人数单价支付并成为团主（48h倒计时）
- 其他已注册用户通过公开链接支付同价加入
- 超时未成团自动触发全员退款

**默认业务规则（先按这个做）**
- 团规模支持 `2~5` 人，价格取 `Course.price_2_persons ~ price_5_persons`
- 成团条件：`paidMemberCount >= targetSize`
- 团主算第一位成员（团主支付成功后计时开始）
- 拼团订单不允许星璇抵扣（先禁用，避免退款冲正复杂度）
- 同一用户同一团只能参一次

**改造计划**

- `1) 数据层（Flyway）`
  - 新建 `group_campaigns`（团主单）：`id, group_no, course_id, leader_user_id, target_size, unit_price, status, expires_at, success_at, failed_at, share_token, created_at...`
  - 新建 `group_members`（成员）：`id, group_id, user_id, order_id, role(LEADER/MEMBER), pay_status, joined_at, refunded_at...`
  - 扩展 `orders`：`biz_type(NORMAL/GROUP)`, `group_id`, `group_role`, `group_expire_at`
  - 关键索引/约束：
    - `uk_group_no`
    - `uk_group_user(group_id, user_id)`
    - `idx_group_status_expires(status, expires_at)`

- `2) 领域模型与仓储`
  - 新增实体：`GroupCampaign`, `GroupMember`
  - 新增仓储：`GroupCampaignRepository`, `GroupMemberRepository`
  - 仓储增加“带锁查询”方法（`PESSIMISTIC_WRITE`）用于并发参团

- `3) 下单&支付接入`
  - 新增开团接口：创建拼团订单（`biz_type=GROUP`，先 `PENDING`）
  - 支付回调成功后：
    - 团主单：创建团并置 `FORMING`，写入 leader member，设置 `expires_at=paidAt+48h`
    - 团员单：加入团，更新已支付人数
    - 若达到人数：原子更新团状态为 `SUCCESS`
  - 不改你现有支付网关主体，只在支付成功编排层分流（`PaymentStatusService`/事件监听）

- `4) 拼团业务接口`
  - `POST /group/open` 开团（返回 orderId/orderNo）
  - `POST /group/join` 参团（入参 shareToken）
  - `GET /group/{groupNo}` 拼团详情（剩余人数、剩余时间、成员）
  - `GET /group/my` 我的拼团（发起/参与）
  - 公开链接采用 `shareToken`，避免裸 ID 枚举

- `5) 48小时超时任务`
  - 新增定时任务每分钟扫描 `FORMING + expires_at<=now`
  - 将团置为 `FAILED`（先落库），再批量触发成员退款
  - 退款结果异步回调后更新成员退款状态；全部成功后团置 `REFUNDED`（或保持 FAILED + 退款完成标记）

- `6) 退款编排（关键）`
  - 复用现有 `RefundApplicationService`，新增“系统自动退款入口”（非用户手动）
  - 幂等键：`groupId + orderId`
  - 失败重试：记录失败原因+重试次数，定时补偿查单
  - 订单状态策略：单成员退款成功后该成员订单 `REFUNDED`

- `7) 钱包/事件一致性`
  - 你当前支付成功会立刻记钱包收入/支出与通知；拼团需改为：
    - 支出（买家）可继续记
    - 卖家收入建议延后到“成团成功”再确认（MVP最稳）
  - 避免“先入账后整团失败退款”导致反向冲正复杂

- `8) 风控与边界`
  - 禁止自己参与自己发起的团（可选，看产品）
  - 团已满/已过期/已失败禁止加入
  - 并发下最后一个名额抢占要原子化
  - 退款中禁止重复触发

- `9) 测试与验收`
  - 单测：状态机、金额计算、超时逻辑、幂等
  - 集成：支付成功回调->成团；超时->批量退款->回调闭环
  - 回归：普通购课链路不受影响（`biz_type=NORMAL`）

**建议开发顺序（7个任务，可并行）**
1. DB迁移 + 实体仓储  
2. 开团/参团下单接口  
3. 支付成功后拼团状态推进  
4. 拼团查询与分享token  
5. 超时任务 + 自动退款入口  
6. 退款补偿与幂等重试  
7. 钱包/通知调整 + 回归测试

**需要你拍板的3个点（会影响实现）**
1. 拼团失败后退款时效：`立即退款` 还是 `T+N分钟批处理`  
2. 拼团期间是否允许使用星璇抵扣（我建议 MVP 禁用）  
3. 卖家收入何时确认：`成团即确认` 还是 `课程完成后确认`（我建议沿用你们现有“课后结算”逻辑）

你回复这 3 点后，我就能给你出“精确到类和方法名”的开发清单。




###

很好，三点已确认，我按这个给你落一版**可直接开工**的方案。

**已锁定规则**
- 拼团失败后：**立即触发退款**（退款执行异步）
- 拼团订单：**不允许星璇抵扣**
- 卖家收入：**课后结算**（沿用你们现有结算节奏）

**技术方案（MVP）**
- 保持现有支付主链路不推翻，在支付成功后增加“普通单/拼团单”分流。
- 拼团以独立聚合建模（`group_campaigns` + `group_members`），`orders` 仅加关联字段。
- 超时任务只负责“判定失败 + 投递退款任务”，退款由异步执行器处理并回写状态。
- 所有关键动作做幂等：支付回调幂等、参团幂等、退款任务幂等、退款回调幂等。

**数据库变更（Flyway）**
- 新表 `group_campaigns`
  - 关键字段：`id, group_no, share_token, course_id, leader_user_id, target_size, unit_price, status, expires_at, success_at, failed_at, refund_finished_at, created_at, updated_at`
  - 状态建议：`FORMING, SUCCESS, FAILED, REFUNDING, REFUNDED`
- 新表 `group_members`
  - 关键字段：`id, group_id, user_id, order_id, role(LEADER/MEMBER), pay_status(PAID/REFUNDING/REFUNDED), refund_status(NONE/PENDING/SUCCESS/FAILED), created_at, updated_at`
- 扩展 `orders`
  - 增加：`biz_type(NORMAL/GROUP)`, `group_id`, `group_role(LEADER/MEMBER)`, `group_expire_at`
- 索引/约束
  - `uk_group_campaigns_group_no`
  - `uk_group_campaigns_share_token`
  - `uk_group_members_group_user(group_id,user_id)`
  - `uk_group_members_order_id(order_id)`
  - `idx_group_campaigns_status_expires(status,expires_at)`

**后端代码改造点（按你项目结构）**
- `entity/`
  - 新增 `GroupCampaign.java`, `GroupMember.java`
  - 扩展 `Order.java` 增加拼团字段与枚举
- `mapper/`
  - 新增 `GroupCampaignRepository.java`, `GroupMemberRepository.java`
  - 增加带锁查询方法（成团与参团并发控制）
- `dto/`
  - 新增：`GroupOpenReqDto`（courseId,targetSize,bookingTime）
  - 新增：`GroupJoinReqDto`（shareToken）
  - 新增：`GroupDetailReqDto`（groupNo/shareToken）
- `vo/`
  - 新增：`GroupOpenVO`, `GroupDetailVO`, `GroupMemberVO`, `MyGroupListVO`
- `service/impl`
  - 新增 `GroupOrderService`（开团/参团下单）
  - 新增 `GroupLifecycleService`（支付后入团、成团判定、超时失败）
  - 新增 `GroupRefundDispatchService`（异步投递退款）
  - 新增 `GroupRefundWorkerService`（执行退款+重试）
- `controller/`
  - 新增 `GroupController`
    - `POST /group/open`
    - `POST /group/join`
    - `GET /group/detail`
    - `GET /group/my`
- `scheduler/`
  - 新增 `GroupExpireScheduler`
    - 每分钟扫描 `FORMING && expires_at <= now`
    - 置团为 `FAILED/REFUNDING` 并投递成员退款任务
- 现有链路改造
  - `PaymentOrderCreationService`：拼团单禁用星璇抵扣
  - `PaymentStatusService`：支付成功后调用 `GroupLifecycleService.onOrderPaid(...)`
  - `PaymentEventListener`：仅 `NORMAL` 单走现有逻辑；`GROUP` 单等成团成功后再触发课程相关通知（或单独模板）
  - `RefundApplicationService`：新增“系统自动退款入口”（不走用户手工校验）

**核心状态流**
- 团主下单支付成功：`FORMING`，`expires_at = paid_at + 48h`
- 团员支付成功加入：计数+1
- 达到 `target_size`：团 -> `SUCCESS`（停止超时退款路径）
- 到期未成团：团 -> `FAILED/REFUNDING`，立即异步发起全员退款
- 成员退款全部成功：团 -> `REFUNDED`

**并发与幂等**
- 参团时：`group_campaigns` 行锁 + 状态/人数二次校验
- 支付回调：按 `outTradeNo` 幂等（你现有已做基础幂等）
- 退款任务：幂等键 `groupId + orderId`
- 退款回调：按 `out_refund_no` 幂等更新

**验收清单**
- 开团支付后返回可分享链接（`shareToken`）
- 两人团：第2人支付后立刻成团
- 超时：48小时到点后团失败并触发全员退款
- 任一退款失败可自动重试，最终状态可追踪
- 普通购课链路不受影响
- 卖家收入仍在课后结算（拼团成功订单按原结算规则进入后续流程）

如果你确认，我下一步就按这个顺序开始出 **第1批代码改造（DB + Entity + Repository + 基础接口骨架）**。