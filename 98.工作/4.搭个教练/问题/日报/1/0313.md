## 今日完成
1. 前后端联调，解决的BUG一览：
    1. 互动足迹 moments/footprints 缺两个参数   
    2. 互动通知 notifications/interactions 缺少返回参数
    3. @好友时没有数据 user-relationships/friends
    4. 不显示连续签到状态 star-points/sign-in/status
2. 部署后端服务器
    - 上述BUG解决后重新部署了后端服务，已在小程序端验证解决
## 遇到问题
1. veImageX 图片审核
    1. 测了一下目前的图片上传+审核，发现并没有`创建审核任务`的逻辑， 系统一直在查一个固定的taskId， 而这个task显示仍在审核中
    2. 上述taskID显示这是一个`新增文件审核(增量审核)`任务，这是一个对已存在的存量图片，按时间 / 目录 / 标签等条件，做小范围批量审核。
        - 不满足项目所需要的 `用户上传图片时审核` 的需求
    3. AI建议使用veImageX的 `实时审核（图片上传至veImageX后自动审核）`和`手动审核（使用API调用审核功能）`功能， 具体怎么用我还在看
## 明日计划
1. 前后端联调（后来测出来的BUG，还没着手研究）
    1. 浏览动态后，浏览历史里面没显示
2. 看看veImageX 图片审核如何接入
3. llm文本安全需要接入并测试






# 临时改动
1. 缺少参数的两个接口
    1. moments/footprints
        - 加一个传参， type 1评论过、2点赞过
        - 少一个返回参数 curser
    2. notifications/interactions
    list 没有返回

    3. /api/categories
        - 应该支持GET方法
    
    4. /api/user-relationships/friends  









2. 接口类型
application/x-www-form-urlencoded




1. 互动通知接口少参数；
    - 添加了性别，位置，专业认证， 兴趣+角色等信息
    - 添加了动态第一张图片的url
2. 签到状态 拿不到连续签到天数；
    - 添加了连续签到天数
    - 添加了上次签到时间
