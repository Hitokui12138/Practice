
#
##
1. 教练实名认证
2. 0. tos sys开头的背景图 没有迁移完毕, 冷热处理的机制
    - 30天没用过。。。就自动删除 sys开头的不处理
3. 动态相关的内容都不需要鉴权
4. 在线与否可以用IM的逻辑
5. 系统消息
2. 课程通知



#
##
1. 身份证二要素认证 使用梦远数据实现
    1. 大陆居民身份证
        1. 输入姓名，身份证号，调用 api/certification/mainland
            - 如果该用户已经有实名认证申请处于 PENDING 或 APPROVED，会直接被拦截
        2. 如果没有历史冲突，进入实名校验，调用 梦远数据 提供的API
        3. 认证成功后落库
            1. certifications， certification_type = MAINLAND
            2. certification_audits， 加一条审核记录
            3. users， 改 isCertified 为true
    2. 港澳台同胞 和 外国人
        - 需要和梦远数据确认是否有这样的功能
        - 阿里云支持港澳台居住证审核，但是不支持外国人护照
    3. 学历认证（暂未实现）

2. 阿里云文本审核 已经接入
    1. RAM用户的信息（已经保存在.env文件里了）
        - AccessKey ID： LTAI5t9oCGaNpjcUcLXHGRvG
        - AccessKey Secret： 7YKuV0R5PrLXywMrqUf28AsRdPYmS
    2. 使用三种模型
        1. ugc_moderation_byllm，用于动态和课程介绍等长文本，异步方法，等图片审核好了一起更新
        2. comment_detection_pro，用于评论，同步调用，检测到违规直接拒绝发送
        3. nickname_detection_pro，用于用户名，同步调用，检测到违规直接拒绝更新用户信息

3. 动态，课程，姓名的审核
    1. 动态和课程
        1. 上传图片进行veImageX异步审核，创建media_file记录
            - 异步接收veImageX的审核结果，直接更新media_file表
        2. 按下发布动态、课程按钮后，调用阿里云文本审核
            1. 若文本审核不通过
                - 直接拒绝发送，也不落库moments或course表
            2. 若文本审核通过，允许发布，然后发一条倒计时1分钟的延迟消息
        4. 一分钟后，根据图片的审核结果：
            1. 若图片审核通过，更新moment缓存
            2. 若图片审核不通过，更新moment表，将这条动态设为私密+违规，且不允许用户修改违规动态/课程的可见性
                - 并更新moment缓存，防止有违规记录存在于缓存中
    2. 姓名和评论（暂未实现）
        1. 更新姓名或发送评论时直接调用阿里云文本审核
            1. 若不通过，直接不允许
            2. 若通过，继续后面的逻辑

4. 即构IM的逻辑
    1. 先给前端发token，然后同步资料
    2. 把 UserRegister 的 user is exists 识别为成功（幂等成功），
    3. 保留 ModifyUserInfo，用于更新昵称/头像

5. 把 获取动态列表，获取动态详情两个API改成无需校验用户登陆状况（用于微信分享）

## 明日计划
1. 把tos上的图片转存到veImageX上
    - 增加冷热处理的机制，30天没用过。。。就自动删除 sys开头的不处理
2. 继续完善剩余内容
    1. 把即构IM的 /api/messages/token 里的逻辑放到用户登录时自动执行
    2. 完善 姓名和评论 的审核
    3. 完善 学历认证
    4. 分享团课时，跳转至动态发布页面然后发布新的动态
3. 性能优化问题
4. 调研 普通商户直连的支付流程如何切换到电商收付通，并实现教练合规提现









## 
1. 第一次购买
    1. 下单 /api/pay/order/create
        {
            "courseId": 23762189762816,
            "amount": 0.01,
            "bookingTime": "2026-03-30 15:30:00"
        }
    - 同时调用支付 /api/pay/create
        {
            "orderId": "31229798048000",
            "channel": "WECHAT"
        }
2. 取消支付
    - 跳转到订单详情页面（因为已经下单了）
    1. 再次支付 /api/pay/create
        {
            "orderId": "31229798048000",
            "channel": "WECHAT"
        }

