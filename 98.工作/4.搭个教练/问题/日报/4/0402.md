#
#4月2日
1. 处理生产环境问题
    1. RabbitMQ报错问题
        - 新的队列无法自动替代旧队列，因此手动删除了旧队列然后重启RabbitMQ
    2. API文档无法打开的问题
        - 一些类的构造方法没有加@Autowired，导致报错
2. 接通火山云短信服务
    - .env中有一个 APP_SMS_REAL_ENABLED 可以控制是否通过
3. 增加星璇购买服务
    1. 新建一个新的星璇订单表（star_point_recharge_orders），以避免影响课程订单表
    2. 新增创建星璇充值订单并发起支付API： /api/star-points/recharge/create
    3. 已测试充值后的支付表payments和星璇记录表star_point_transactions的更新情况
4. 小程序审核前测试
    1. 手机号注册登录
    2. 实名认证，学历认证
5. 整理微信支付所需的配置以及在网站上对应的位置



#
# 明日
1. 允许用户未登录状态下访问以下画面（鉴权）
    1. 点击其他用户头像，看别的用户详情 /api/users/info/26012800001
        1. 动态 
        2. 课程
        3. 认证
        4. 评价
    2. category/my, 给未登录的用户返回所有兴趣列表
    3. 课程打开时不要报错



2. 注销条件
    1. 课程应该都是已结束
    2. 钱包只是记账，微信那边的钱要已经跑完
    3. 7天冷静期（已经设置）软删除
        - 7天内可以再登录回来
            - 把软删除改回来
    4. 定时 每天执行一次

3. 7天正式注销后
    1. 手机号，微信ID，苹果ID之类的 从数据库中清除（删除记录或者字段）
        - 便于新注册（用户ID变了）
    2. 认证信息
        - 姓名 身份证，学历认证等 可以明文+脱敏，软删除
        - 再注册的话不要复用，只是存在数据库里而已
        - 再次注册需要重新认证
    3. 动态 设为私密，软删除
    4. 课程 下架 + 软删除
    5. 交易信息要完整保留 + 软删除

4. 最大发10条动态的设置

# 推荐算法
- 先在本地找一个开源的


# 用户注册后
- 默认官方用户要关注此人

# 把小程序版本保存在字典里





# 推广课程
1. 推广的是 收藏的课程
2. 收藏



# 
1. 申请资格认证 /api/qualification-certification/apply
2. 申请其他认证 /api/other-certification/apply
3. 申请荣誉认证 /api/honor-certification/apply
4. 申请学历认证 /api/education-certification/apply
5. 大陆居民实名认证 /api/certification/mainland
    - 仅身份证和
6. 港澳台实名认证 /api/certification/hmt
7. 外国人认证 /api/certification/foreigner
8. 取消认证申请 /api/certification/cancel/{certificationId}