#
1. 注释掉持久化这段
```docker-compose
      start_period: 90s
    # volumes:
    #   - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - skillup-network
```
2. 执行`docker compose down -v`删除旧卷
3. `docker compose up -d`
    - 下次应用启动会按你代码里的新参数重新声明队列（含 x-dead-letter-exchange）。