可以，下面给你一版**可直接提交**的工单正文（已按你提供的证据整理好）。

**工单标题**
`ws.skillupsh.com:443 TLS 握手被重置，导致 WSS 无法建立（疑似链路/边界策略问题）`

**问题描述**
- 业务场景：Java 后端 WebSocket，经 Nginx 暴露 `wss://ws.skillupsh.com/ws`
- 现象：
  - `ws://115.191.4.192:8081/ws` 可连接（明文直连）
  - `wss://ws.skillupsh.com/ws` 在 APIFox 报 `1006 Abnormal Closure`
  - 客户端命令复现失败：
    - `curl -vk https://ws.skillupsh.com/ws` -> `Recv failure: Connection was reset`
    - `openssl s_client -connect ws.skillupsh.com:443 -servername ws.skillupsh.com` -> `no peer certificate available`，`handshake read 0 bytes`

**已完成自查（源站正常）**
- Nginx 配置与进程正常：
  - `nginx -t`：syntax ok / test successful
  - `systemctl status nginx`：`active (running)`
- 源站本机 TLS 正常：
  - `openssl s_client -connect 127.0.0.1:443 -servername ws.skillupsh.com -showcerts`
  - 成功返回证书链，`TLSv1.3`，`Verify return code: 0 (ok)`

**抓包证据**
- 抓包命令：`tcpdump -i any -nn -s 0 -vvv -X 'host 61.171.132.23 and port 443'`
- 抓包结论：
  - TCP 三次握手成功
  - 客户端发送 TLS `ClientHello`（SNI=`ws.skillupsh.com`）
  - 服务端仅回 ACK 后，客户端方向立即出现 RST（重复）
  - TLS 握手在服务端发证书前被中断
  - RST 报文 TTL 与前序入站包 TTL 不一致（有中间链路设备干预可能）

**初步判断**
- 问题不在 Java WebSocket 代码层，也不在 Nginx 进程/语法层；
- 疑似客户端到源站 `443` 路径中的网络/边界策略导致 TLS 握手被重置（RST）。

**请求协助排查**
请云网络/安全侧协助排查并确认：
- 安全组、网络 ACL、云防火墙、高防、WAF、边界设备是否对 `115.191.4.192:443` 或 `SNI=ws.skillupsh.com` 存在拦截/注入 RST
- 运营商链路是否存在策略性重置
- 是否有策略仅允许特定来源访问 443 TLS

**排查对象**
- 客户端公网 IP：`61.171.132.23`
- 目标域名：`ws.skillupsh.com`
- 目标 IP：`115.191.4.192`
- 目标端口：`443`
- 发生时间：`2026-04-23 11-16-48`

**附件**
- 客户端 `curl -vk` 输出
- 客户端 `openssl s_client` 输出
- 源站 `tcpdump` 关键片段
- `nginx -t` 与 `systemctl status nginx` 输出

如果你需要，我也可以再给你一版“简短工单版（5-8行）”用于IM群里先拉人处理。