1. 创建订单链（不发起支付）
    0. @SentinelResource(value = "createOrder", blockHandler = "handleCreateOrderBlock")
        - 阿里Sentinel的注解， 资源名， 限流/降级兜底方法， 还可以配置`业务异常时兜底的方法`
        - 类似于ExceptionHandler， 限流降级后抛出异常
    1. 参数
        - @RequestBody CourseOrderCreateReqDto 课程ID，预约时间，金额， 积分
        - @AuthenticationPrincipal UserPrincipal
            - 快速从当前认证上下文（SecurityContext）中获取已登录用户的「主体信息」（Principal）
    2. 性能测算服务？ `performanceMetricsService.startSample();`
- `PaymentController.createOrder`
- `PaymentOrderCreationService.createCourseOrder`
    - 创建订单paymentOrderCreationService.createCourseOrder， 主要是校验
- `OrderServiceImpl.createCourseOrder`
    - 校验课程/角色/性别友好配置/体验课规则
    - 计算金额（原价、体验课折扣、星璇抵扣）
    - 如使用星璇，先调用 `StarPointService.deductStarPoints`
    - 落库 `Order(status=PENDING)`， JPA save()
- `PaymentBookingLockService.lockCourseBookingSlots`
- 落库 `BookingRecord(status=LOCKED, orderId=...)`

2. 发起支付链（按订单选渠道）
- `PaymentController.createPayment`
    - `PaymentApplicationService.createPaymentByOrder`
        1. 获取订单号？
        - `PaymentOrderCreationService.createPaymentByOrder`
            1. 校验， 先查订单号是否存在
                - 校验订单归属与状态必须为 `PENDING`
                - 创建/复用 `PaymentRecord(tradeStatus=PENDING)`
            2. 依据 `channel` 走网关： `platform` 枚举类： PaymentRecord
                - 结合 `payDataFactory.createPayData`, 创建相关数据
                    - `ALIPAY/ALIPAY_SANDBOX` -> `AlipayServiceImpl.createTradePagePay*` 
                    - `WECHAT` -> `WeChatPayServiceImpl.createOrder`
                    - `UNIONPAY` -> `UnionPayServiceImpl.createOrder`
                    - `DOUYIN` -> `DouyinPayServiceImpl.createOrder`
            2. 创建Payment
            3. 创建payData
            4. 更新Order
- 返回 `PaymentCreateVO(order + payment + payData(支付宝表单、微信支付参数))`


#
## PaymentController
### 订单
1. createOrder
2. cancelOrder
### 支付
3. createPayment 前端选择支付方式后，通过订单ID发起支付并`返回 payData`
    1. paymentApplicationService.createPaymentByOrder(userPrincipal.getId(), request);
        2. return PaymentOrderCreationService.createPaymentByOrder(request.getChannel(), this::createPayData)
            - PaymentPayDataFactory this::createPayData 
            1. save（payment）
            2. Object payData = payDataFactory.createPayData(platform, order, record);
4. wechatNotify 微信支付回调

## createPayData
- String alipayService.createTradePagePaySandbox(PaymentRequestBuilder.buildAlipayRequest(record));
- weChatPayService.createOrder(PaymentRequestBuilder.buildGatewayRequest(order, record));
### AlipayServiceImpl
1. AlipayClient alipayClient = createAlipayClient()
2. AlipayTradePagePayRequest alipayRequest = getAlipayTradePagePayRequest()
    - 创建request页面， 固定值， 特殊字段
3. AlipayTradePagePayResponse response = alipayClient.pageExecute(aliRequest)
#### 
1. 配置类
    - AlipayConfig， AlipaySandboxConfig implements AlipayClientProperties
        - 可能是某种最佳实践
2. Ali包提供
    1. AlipayClient
    2. AlipayTradePagePayRequest
    3. AlipayTradePagePayResponse