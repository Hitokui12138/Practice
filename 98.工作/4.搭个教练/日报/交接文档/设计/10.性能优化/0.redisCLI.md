# INFO stats
1. 缓存命中率
    - keyspace_hits:    82    ← 缓存命中次数
    - keyspace_misses: 147    ← 缓存未命中次数
    1. 缓存命中率 = 82 / (82 + 147) = 35.8%
        - 正常缓存命中率应该 ≥ 90%
        - 你只有 35% → 严重偏低
2. 缓存过期
    - expired_keys: 106
    - 106 个 key 已经过期 → 大量缓存失效，必须重新查数据库 → 接口变慢。
3. redis发生错误
    - total_error_replies: 8
    - Redis 有 8 次错误响应 → 对应 openCode 说的：



# TTL 最佳实践
1. 慢 SQL、统计、计数 → 5~30 分钟
2. 用户资料 → 5~10 分钟
3. 关系、列表 → 3~10 分钟
4. 配置 → 1~24 小时
5. 锁、临时状态 → 1~5 秒
6. 实时强依赖 → 10~30 秒