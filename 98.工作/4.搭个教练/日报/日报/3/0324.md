## 项目中临时修改的位置，记得删
1. openId resolveOpenid(userId, request == null ? null : request.getChannel());







## 今日完成
1. 前后端联调，解决的BUG：
    1. 下单后先不支付，然后在订单详情页面再支付时遇到的报错问题
2. 功能添加
    1. 团课优惠功能，已实现功能并对下面场景进行了测试：
        - 最终价 = 原价 * 体验折扣率 - 星璇抵扣金额
        1. 团长开团时，用星璇抵扣费用
        2. 团员入团时，使用新客优惠
        3. 未成团自动退款，钱和星璇都退
    2. 0元订单功能，已实现功能并对下面场景进行了测试：
        1. 团员入团是，使用新客优惠+星璇抵扣，创建0元订单
        2. 0元订单退款，仅退还星璇
    3. 加了一个字典表 sys_dictionary
        - 后端使用SpringCache的注解对字典表数据进行缓存
        - 前端可以传入多个type来获取目标页面所有下拉框的选项
    4. 加了一个退款详情页面接口
        - 现在可退款金额和实际支付金额一致，之后可以在这个接口里计算可退款的金额
## 遇到问题
- 暂无
## 明日计划
1. 新需求：
    1. 学习日历需要返回上课表（course_lesson_details）记录，不是订单表
    2. 审核功能 先发布，后异步审核
    3. 加一个教练给学员退款的功能，如果是团课，则给全员退团（防止低价参团再退团）
    4. 分享团课时，跳转至动态发布页面然后发布新的动态
2. 调研 火山云和阿里云的 文本审核
3. 性能优化问题


id（主键）	
type_code	（字典类型，如 GENDER、HONOR_LEVEL）
item_code	（编码，如 1、NATIONAL）
item_label	（展示文案，如“男”、“国家级”）
item_value	用longtext类型，可能存放长文本
sort_order（排序）	10，20
is_active（是否启用）	1启用，0不启用
created_at	创建时间
updated_at	更新时间



# 分享页面
1. 拼课的分享 拼团详情
    - 再加一个跳转课程详情的按钮
    - 分享 跳转到发动态的页面， 绑定一个主题（#大家一起来参团）和一个分类（网球之类的）不能改， 自动生成一个动态
        - 内容可以自定义
2. 推广课程 扣积分