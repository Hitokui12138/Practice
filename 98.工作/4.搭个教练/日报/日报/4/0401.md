1. 允许用户未登录状态下访问以下画面（鉴权）
    1. 点击其他用户头像，看别的用户详情
        1. 动态
        2. 课程
        3. 认证
        4. 评价

4. 今日API耗时排行：
【接口耗时排行（按最大耗时降序）】
排名   是否慢接口      接口URI                                              请求次数     最大耗时(秒)      平均耗时(秒)     
----------------------------------------------------------------------------------------------------
1    ✅          /api/moments/recommended                           59       2.2889       1.4222      
2    ✅          /api/moments/comments/add                          10       2.2297       1.6221      
3    ✅          /api/users/my                                      76       1.578        1.1045      
4    ✅          /api/moments/comments/list                         17       1.3056       1.0131      
5    ✅          /api/moments/detail                                8        1.1915       1.2882      
6    ✅          /api/notifications/interactions                    8        1.1846       1.2524      
7    ❌          /api/notifications/interactions/mark-all-read      1        0.6704       0.6704 




# 今日
1. 下面所有动态列表都有问题
    1. 新发出来的动态不是第一条
        - 让用户刚刚发布的（30秒内）动态总是排在第一位，之后再进推荐算法
    2. 有些动态一直刷不到
        - 扩大了全局候选 ID 池（300条），现在最新的300条是可以刷出来了的
    3. 往上刷新应该刷出新的东西，反向瀑布流
        - 给用户单独维护一个redis-set（TTL24h）， 确保用户一天内刷新后看到的东西不重复
    4. 分类查询查不出来（匿名用户问题）
        1. 互动，原用户还是要能接收到
            - 回复的话还是原逻辑（未实现）
        2. 用户名和头像是假的（已经实现）
2. 推荐算法 /api/moments/recommended
    1. 首先获取近期300条候选ID
    2. 然后用户调用动态时，根据算法推荐给用户
        - 保存用户看过的动态ID到redis的Set中（TTL为24小时）
    3. 用户下拉刷新时，用Set确保用户不会刷到重复内容
3. 其他前后端联调的BUG
4. 性能优化（未上线，本地测试中）
    1. /api/users/my
        1. 原因：
            1. 缓存TTL短， 只有30s
            2. 落库查了7次
                - 落库链路是串行多查询：src/main/java/com/dgjl/skillup/service/impl/UserMySummaryService.java:50 起，至少 7 次查询（用户、3个关系count、访客count、积分、钱包）。
            3. COUNT(DISTINCT) 可能是主要 SQL 热点：countDistinctVisitorsByVisitedId 在大表上可能慢；当前迁移里没看到 (visited_id, visitor_id) 这种直击该查询的索引。
                - COUNT(DISTINCT) 这种慢查询


# 明日
1. 允许用户未登录状态下访问以下画面（鉴权）
    1. 点击其他用户头像，看别的用户详情
        1. 动态
        2. 课程
        3. 认证
        4. 评价

2. 注销条件
    1. 课程应该都是已结束
    2. 钱包只是记账，微信那边的钱要已经跑完
    3. 7天冷静期（已经设置）软删除
        - 7天内可以再登录回来
            - 把软删除改回来
    4. 定时 每天执行一次

3. 7天正式注销后
    1. 手机号，微信ID，苹果ID之类的 从数据库中清除（删除记录或者字段）
        - 便于新注册（用户ID变了）
    2. 认证信息
        - 姓名 身份证，学历认证等 可以明文+脱敏，软删除
        - 再注册的话不要复用，只是存在数据库里而已
        - 再次注册需要重新认证
    3. 动态 设为私密，软删除
    4. 课程 下架 + 软删除
    5. 交易信息要完整保留 + 软删除

4. 最大发10条动态的设置

# 新需求
1. 下面所有动态列表都有问题
    1. 新发出来的动态不是第一条
        - 让用户刚刚发布的（30秒内）动态总是排在第一位，之后再进推荐算法
    2. 有些动态一直刷不到
        - 扩大了全局候选 ID 池（300条），现在最新的300条是可以刷出来了的
    3. 往上刷新应该刷出新的东西，反向瀑布流
        - 给用户单独维护一个redis-set（TTL24h）， 确保用户一天内刷新后看到的东西不重复
    4. 分类查询查不出来（匿名用户问题）
        1. 互动，原用户还是要能接收到
            - 回复的话还是原逻辑
        2. 用户名和头像是假的

2. 最大发10条的


- 
1. 推荐算法 /api/moments/recommended
    1. 缓存， 有 Redis 缓存和预加载下一页
        - 缓存分组 moments_preload
        - su:moments:rec:cursor:mv{ver}:u{userId}:c{cursor|first}
    2. 逻辑
        1. 候选集：近 30 天公开可见动态，按发布时间倒序取候选 ID
        2. 排序：使用统一打分函数 calculateScore
2. 地区 /api/moments/list-by-location
    1. 
        - 缓存分组 moments_preload
        - su:moments:location:cursor:v{ver}:r{regionId}:c{cursor|first}
    2. 
        1. 候选集：regionId + 子地区 的动态（先扩展地区列表），查询候选 ID 使用地区+可见性规则+黑名单过滤
        2. 排序： 候选 ID 初始按 id DESC， 再进入同一 calculateScore 重排
3. 分类 /api/moments/list-by-category
    1. 
        - 缓存分组：moments_by_category
        - su:moments:category:v{ver}:u{userId}:c{categoryId}:p{page}:l{limit}:cursor:{cursor}
    2. 
        1. 候选集： 该分类下、满足可见性/关系规则的动态 ID（自定义 Criteria）
        2. 排序：同样走 calculateScore
        3. 分页： 分页形态：先取 pageSize+1，再由 CursorPagingSupport 计算 hasMore
4. 话题 /api/moments/list-by-topic
    1. 推荐
        - 打分模型
    2. 最新
        - 走“最新”流，直接 createdAt DESC, id DESC，不做推荐打分

5. 关注列表先按时间展示 /api/moments/list-by-followed-users
6. 我的也是按时间 /api/moments/my-list
7. 看别人按时间 /api/moments/list-by-user-with-pin




# 
1. 退款申请
2. 预约时间
    1. 学生约教练不用申请
    2. 教练约学生需要申请



# 
1. 注销条件
    1. 课程应该都是已结束
    2. 钱包只是记账，微信那边的钱要已经跑完
    3. 7天冷静期（已经设置）软删除
        - 7天内可以再登录回来
            - 把软删除改回来
    4. 定时 每天执行一次

2. 7天正式注销后
    1. 手机号，微信ID，苹果ID之类的 从数据库中清除（删除记录或者字段）
        - 便于新注册（用户ID变了）
    2. 认证信息
        - 姓名 身份证，学历认证等 可以明文+脱敏，软删除
        - 再注册的话不要复用，只是存在数据库里而已
        - 再次注册需要重新认证
    3. 动态 设为私密，软删除
    4. 课程 下架 + 软删除
    5. 交易信息要完整保留 + 软删除

#
1. 认证分成自动认证和手动认证
    - 前端仅传subtype，后端自动走路径