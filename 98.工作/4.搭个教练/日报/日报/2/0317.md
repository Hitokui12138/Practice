## 今日完成
1. 前后端联调，解决的BUG一览：
    1. 我看过谁栏目里面没有数据
    2. 获取兴趣里面没有数据
    3. 上传课程图片时提示图片类型非法
2. 媒体上传审核功能已改完，现在正在测试
    1. （已测试）`要审核` usage=1 头像
        （已测试）- 背景图的usage也是1，但是走的另一个接口
    2. `要审核` usage=2 动态媒体（图/视频/语音）动态发布请求里绑定 mediaFileId：
        1. （已测试）图片
            1. 如果显示
        2. 视频
        3. 音频
    3. `要审核` usage=3 课程图片，课程创建使用 courseImages[].mediaFileId：
    4. (不用AI审核) usage=4 认证材料
    5. `要审核` usage=5 音频文件，后端有音频分类和波形处理链路
    6. (不用AI审核) usage=6 举报凭证（后端支持）
## 遇到问题
- 暂无，我没有违规的视频音频样本，但是后端对违规媒体处理逻辑是一样的（标记为不安全，并删除数据库中的file_url确保用户拿不到地址）
## 明日计划
1. 继续测试剩余几种上传途径
2. 更新 智能审核 的审核策略， 忽略路径开头为"user/auth"和"user/report"的文件(限制只审核需要审核的四种文件)





ImageXService 变成 VeImageXService


VeImageXAuditTaskService 可能不需要这几个类
VeImageXAuditTaskServiceImpl 上面那个实现类
FileManagementServiceImpl 在用上面的方法
UploadAuditServiceImpl 删掉了审核相关方法，之后手动审核加在这里


https://644de2fc.r11.cpolar.top
644de2fc.r11.cpolar.top/api/audit/veimagex/notify


http://${${VOLC_IMAGEX_CUSTOM_DOMAIN:${VOLC_IMAGEX_CDN_DOMAIN:}}}/tos-cn-i-/user/moment/20260317/e/a/ea92bad726b2459ea7b02f6fcfcdd367.jpg~tplv-nl7c3vr9x7-user.webp
http://imagex.skillupsh.com/tos-cn-i-nl7c3vr9x7/user/moment/20260317/6/b/6bbc6d04ada44318a2f6fbd8b2b2035c.jpg~tplv-nl7c3vr9x7-webp_watermark_compress.webp



# veImageX
1. 上传流程，服务端上传时序图如下图所示，整体分为三步：
    1. 业务服务端发起 ApplyImageUpload请求，请求获取文件上传和上传凭证。
        ```java
        ApplyImageUploadRequest request = new ApplyImageUploadRequest();
        request.setServiceId("service id"); // 服务 ID
        // request.setUploadHost("upload host");
        // request.setOverwrite("False");
        try {
            // 读取文件
            List<byte[]> datas = new ArrayList<>();
            datas.add(Files.readAllBytes(Paths.get("image path 1")));
            datas.add(Files.readAllBytes(Paths.get("image path 2")));

            // 上传文件
            CommitImageUploadResponse response = service.uploadImages(request, datas);
            System.out.println(response);
        } catch (Exception e) {
            e.printStackTrace();
        }
        ```
    2. 业务服务端使用公网域名上传文件二进制数据。通过对步骤 1 Apply 返回的上传地址发起 PUT 请求上传文件，因使用的公网域名，对上传机房无限制。
    3. 业务服务端发起 CommitImageUpload上传结果确认请求，veImageX 上报上传完成。
2. 



# 文件上传流程
1. /api/files/upload TosController
    0. 审核，拦截
    1. 动态 file(binary)，usage 2
        - 在这里判断走哪个上传路径
    2. fileManagementService.uploadMediaFiles(files, null, userId, usage);
2. FileManagementService
    1. uploadMediaFiles(List<MultipartFile> files, String mediaType, Long userId, Integer usage)，上传文件
    2. detectMediaType， 检测文件类型
    3. getStoragePath， 获取存储路径
    4. checkFileExists， 检测文件是否存在， 基于MD5校验
    5. calculateFileMd5， 计算文件MD5的值
    6. setCourseImageCover， 设置课程封面
    7. deleteTosFileByMediaFileId， 删除文件
3. 基础的上传功能
    1. TosConfig
    2. TosService
        - 里面的上传方法是基础上传方法，下面几个类都在用
        1. TosFileStorageServiceImpl
        2. FileManagementServiceImpl （最复杂那个类）
        3. FileManagementServiceImplBackfillTest
        4. FileManagementServiceImplReportUploadTest 



1. 
```java
public class ImageXConfig {

    private boolean enabled = false; 这个要配置成true

      imagex:
    enabled: true
```




