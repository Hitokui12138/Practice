## 1. 四种事务隔离级别
1. READ-UNCOMMITTED(读取未提交) 
2. READ-COMMITTED(读取已提交) 
    - 解决脏读问题, 读取到别人没有提交的修改
3. REPEATABLE-READ(可重复读)
    - 解决不可重复度问题, 一个事务中, 两次读取结果的值不一致
4. SERIALIZABLE(可串行化) 
    - 解决幻读问题, 一个事务中, 两次读取结果的条数不一致
