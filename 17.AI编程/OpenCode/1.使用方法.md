#
1. `/init`, 创建和更新Agents.md文件
    1. 第一次运行时，OpenCode会扫描这个项目，记录项目结构，代码规范等等
    2. 后续再运行时，会尝试在此基础上进行补充
    3. 可以手动在Agens.md文件中加入内容
        1. 定义AI的角色定位（指定其为Golang程序员）
        2. 约束代码风格
        3. 规定交互规则（确认后再执行，删除前需确认）
    4. OpenCode支持从多个位置读取Agents.md