1. MyBatis 分页与滚动加载
    1. 分页
        - PageHelper 插件, PageHelper.startPage(pageNum, pageSize);
        - MyBatis 拦截器，在 SQL 执行前自动拼接LIMIT pageNum, size
    2. 滚动加载
        - pageSize是已知的, 只要根据最后一个项目的ID, 计算一下当前Page数即可
        - int pageNum = (lastId / pageSize) + 1;